# Pinned so the Agent Engine build gets recent-enough deps. The console
# Dashboard + the Identity / Topology / Security tabs stay LOCKED unless the
# deployed runtime has google-cloud-aiplatform >= 1.126.1 and google-adk >=
# 1.18.0. aiplatform is otherwise pulled in transitively at an older version,
# so we pin it explicitly.
google-adk>=1.31.0
google-cloud-aiplatform>=1.126.1
google-cloud-firestore>=2.0.0
# Required for --otel_to_cloud: ADK's GCP telemetry setup imports the OTLP/HTTP
# span exporter at startup; without these the managed runtime fails to start.
opentelemetry-exporter-otlp-proto-http>=1.42.1
opentelemetry-exporter-gcp-trace>=1.12.0
